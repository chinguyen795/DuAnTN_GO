@* @model DuAnTN.Models.Address

@{
    ViewData["Title"] = "Đổi Địa Chỉ Giao Hàng";
}

<!-- Hiển thị thông báo từ TempData -->
@if (TempData["ToastMessage"] != null)
{
    <div class="alert alert-@TempData["ToastType"]" role="alert">
        @TempData["ToastMessage"]
    </div>
}

<div class="container mt-4">
    <h2>📝 Đổi Địa Chỉ Giao Hàng</h2>

    <form method="post">
        <div class="form-group">
            <label for="NameAddress">Số nhà/Tên Đường</label>
            <input type="text" class="form-control" id="NameAddress" name="NameAddress" value="@Model.NameAddress" required />
        </div>

        <div class="form-group">
            <label for="Description">Địa chỉ (Đọc chỉ) </label>
            <input type="text" class="form-control" id="Description" name="Description" value="@Model.Description" readonly />
        </div>

        <button type="submit" class="btn btn-primary">Lưu Địa Chỉ</button>
    </form>
</div>
 *@