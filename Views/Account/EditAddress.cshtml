@* @model DuAnTN.Models.Address

@{
    ViewData["Title"] = "Chỉnh Sửa Địa Chỉ";
}

<!-- Hiển thị thông báo từ TempData -->
@if (TempData["ToastMessage"] != null)
{
    <div class="alert alert-@TempData["ToastType"]" role="alert">
        @TempData["ToastMessage"]
    </div>
}

<div class="container mt-4">
    <h2>✏️ Chỉnh Sửa Địa Chỉ</h2>

    <form method="post">
        <div class="form-group">
            <label for="NameAddress">Tên Địa Chỉ</label>
            <input type="text" class="form-control" id="NameAddress" name="NameAddress" value="@Model.NameAddress" required>
        </div>

        <div class="form-group">
            <label for="Province">Tỉnh/Thành phố</label>
            <select class="form-control" name="Province" id="Province" required>
                @foreach (var province in ViewBag.Provinces)
{
    @{
        var isSelected = province.name == Model.Description.Split(',')[0] ? "selected" : "";
    }
    <option value="@province.name" @Html.Raw(isSelected)>@province.name</option>
}

            </select>
        </div>

        <div class="form-group">
            <label for="District">Quận/Huyện</label>
            <input type="text" class="form-control" name="District" value="@Model.Description.Split(',')[1]" required>
        </div>

        <div class="form-group">
            <label for="Ward">Phường/Xã</label>
            <input type="text" class="form-control" name="Ward" value="@Model.Description.Split(',')[2]" required>
        </div>

        <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
    </form>
</div>
 *@